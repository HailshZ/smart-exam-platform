import React from 'react';
import { Container, Alert } from 'react-bootstrap';

function ExamInterface() {
  return (
    <Container className="mt-4">
      <Alert variant="info">
        <h4>Exam Interface</h4>
        <p>This is where students will take exams. The full exam functionality will be implemented here.</p>
      </Alert>
    </Container>
  );
}

export default ExamInterface;
